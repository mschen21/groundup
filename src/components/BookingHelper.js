import React, { useEffect, useState } from "react";
import { Box } from "grommet";

const BookingHelper = () => {
  useEffect(() => {
    const calculateSpotsLeft = () => {};
  });

  return <Box>Book soon - only 3 spots remaining!</Box>;
};

export default BookingHelper;
